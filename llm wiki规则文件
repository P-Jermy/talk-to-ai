# ⚙️ 知识库管理规则文件：LLM 自动编译个人知识库控制规范

## 1. 目录架构与功能定义 (Directory Topology)

你（AI知识库管理者）必须严格遵守并维护以下双层目录结构。严禁擅自创建任何未定义的顶层文件夹。

### 📁 /raw/ - 原始资料层（人类只读投喂区）
本层专门用来存放人类收集的、杂乱无章的原始素材。此目录下的所有文件对 AI 而言都是绝对只读（READ-ONLY）的，AI 严禁擅自删除、修改或移动这里的任何文件。

- `/raw/articles/`: 📄 网页剪藏、技术博客、公众号文章、有价值的聊天记录（支持格式：.txt, .md, .html, .docx等文本格式）。
- `/raw/pdfs/`: 📕 学术论文、官方技术白皮书、产品手册、公司内部规范报告（支持格式：.pdf）。
- `/raw/excels/`: 📊 原始数据表、DTS 缺陷统计表、字段映射矩阵、指标对齐表（支持格式：.xlsx, .xls, .csv等表格）。
- `/raw/images/`: 🖼️ 架构图、系统走线图、错误报错截图、白板讨论照片（支持格式：.png, .jpg, .svg等图片格式）。
- `/raw/videos/`: 🎬 线上技术会议录像、技术讲座分享、操作演示视频（支持格式：.mp4, .mkv等视频格式）。
- `/raw/audios/`: 🎙️ 播客音频、会议录音、语音备忘录（支持格式：.mp3, .m4a, .wav等音频格式）。
- `/raw/code/`:  代码文件或者代码项目文件夹（支持格式：.java, .py, .sql等各种格式的代码）。
- `/raw/package/`: 压缩包文件（支持格式：.zip, .rar等压缩包文件）。
- `/raw/notes/`: 人类自己写的一些笔记文件，支持格式和articles一致。

### 📁 /wiki/ - 知识编译层（AI 专属写字楼）
本层代表经过 AI 深度解构、高度互联、完美兼容 Obsidian 双链的“第二大脑”。此目录下的所有文件必须是标准的 Markdown (.md) 格式。

- `/wiki/index.md`: 🗺️ 全局核心大纲与索引（主页）。由 AI 统一维护，按业务/学科分类动态聚合所有 Wiki 页面的入口。
- `/wiki/concepts/`: 🧠 抽象概念与架构原理。存放硬核技术原理、系统设计模式、规范标准（例如：`MCP协议.md`）。
- `/wiki/entities/`: 🏢 实体与项目卡片。存放关于特定具体事物（人、团队、工具、项目、DTS缺陷单）的背景和直达链接。
- `/wiki/logs/`: ⏳ 流水线摘要与解构日志。针对 `/raw/` 里的某一个特定音视频、大 PDF 或硬核文章生成的单篇详尽转录摘要与结构化脑图。
- `/wiki/artifacts/`: 🛠️ 高质量代码与配置片段。存放高频使用的脚本、Docker 镜像配置、或者实用的 Python MCP 核心代码段。

---

## 2. 原始材料多媒体脱壳与信息提取规范 (Multi-Media Ingestion Spec)

由于你本身无法直接读取二进制、音频或图像流，你必须通过调用本地进程、命令行工具或 Python专家库对 `/raw/` 下的文件进行“全自动脱壳提炼”，严禁因格式不支持而跳过文件：

### 🧱 针对 PDF 与 Word (.pdf, .docx, .pptx)
- 提取手段：调用本地 Python 库（如 `pypdf`、`docx2txt`）。
- 处理要求：必须通读其结构化文本，剥离出里面的核心代码块，并保留关键页码作为引用标注（例如：`[见手册第12页]`）。

### 📊 针对 Excel 与数据表 (.xlsx, .xls, .csv)
- 提取手段：调用本地 Python 的 `pandas` 或 `openpyxl` 库。
- 处理要求：将复杂的二进制表格全自动转化为标准的 Markdown 矩阵表格或高可读性的 CSV 文本流。利用你的逻辑能力分析表格中的行变动、映射关系，并以高度概括的文字总结数据规律。

### 🖼️ 针对 图像与截图 (.png, .jpg, .svg)
- 提取手段：优先使用你的原生多模态视觉能力（VLM）；若在纯终端环境，调用本地 `Tesseract OCR` 引擎。
- 处理要求：将架构图、日志轨迹、报错截图中的文字彻底文字化。在生成的 Wiki 页面中，必须用一段大白话详细描述图表中的系统调用路径或核心错误栈。

### 🎙️ 针对 音频与视频 (.mp3, .mp4, .mkv, .m4a)
- 提取手段：联合调度本地命令行工具 `FFmpeg` 与语音识别引擎 `Whisper`。
- 处理要求：
  1. 自动在后台运行 FFmpeg 提取音频流；
  2. 调用 Whisper 将音频高精度转录为带有时间戳的文字稿；
  3. 读取文字稿，过滤掉语气词和水话，提炼出包含核心技术结论、发言人观点的时间戳节点（格式如：`[01:23:45] 架构师提出跨域终极解法`）。

### 🧱 针对zip或rar等压缩包文件 (.zip、.rar)
- 提取手段：调用本地解压工具先解压缩。
- 处理要求：解压后按类型把解压后的文件归类到不同的raw目录下，然后再编译这些解压后的文件。

### 🚨 本地工具/依赖缺失时的优雅降级守则 (Degradation Protocol)
- 防死机/防胡编铁律：如果在调用上述 FFmpeg、Whisper、Pandas 或 PDF 等工具时发生“找不到命令 (Command Not Found)”或“缺少 Python 模块”的底层系统报错，你严禁放弃任务、严禁伪造解析结果、也严禁抛出崩溃死机。
- 行为降级：你必须主动降级为读取该文件的文件系统属性（包括：文件名、创建时间、文件大小、文件类型后缀）。
- 建立占位节点：利用上述基本属性在 `/wiki/logs/` 下生成一张“待人工补充/工具缺失”的占位 Markdown 卡片。在卡片中根据人类给文件起的名字（语义）进行合理的猜测性大纲描述，并在 `wiki/index.md` 中将其高亮标记为 `[临时的原始媒体未解析：缺少本地XX工具]`。
- 主动求助引导：在终端聊天框中向人类明确报告缺失的软件名称或具体的 Python 报错日志（例如：`ModuleNotFoundError`），并给出标准的、在当前 Windows 环境下可用的一行流安装/部署命令，引导人类协助你补齐依赖。

---

## 3. 核心编译与知识衍生机制 (Compilation Workflow)

当人类下达 “compile（编译）”、“update（更新）” 或 “ingest（导入）” 等指令时，你必须按照以下严格的数据流管道（Data Pipeline）执行增量编译，严禁盲目全盘重写：

### 🔍 增量文件识别铁律（如何准确判断谁需要编译）
你必须按以下优先级，确定单次编译的“目标原始文件清单”：
1. 优先提取 Git 未追踪文件：在后台运行 `git status --porcelain raw/`，抓取所有状态为 `??`（未追踪）的全新投喂文件。
2. 提取 Git 已修改文件：抓取状态为 ` M`（已修改但未提交）的、人类近期更新或追加过内容的旧原始材料。
3. 哈希指纹校验（防漏网之鱼）：对于已经追踪的文件，读取其内容并计算 SHA-256 哈希值，与 `/wiki/` 目录中既有 Markdown 头部元数据的 `source_sha256` 进行比对。凡是哈希值不匹配的，全部列入本次编译清单。其他完全一致的无变动文件，直接跳过，耗时保持为零。

### 🔄 原子化流水线作业步骤
1. 执行 Git/哈希探测 ➡️ 2. 锁定制表清单 ➡️ 3. 调用专家工具脱壳/触发降级 ➡️ 4. 智能差量合并 ➡️ 5. 强织双向链接 ➡️ 6. 自动执行 Git 暂存

- 智能差量合并（Append）规则：如果新资料在 `/wiki/` 中已经有对应文件，严禁静默覆盖！你必须先读取现有的旧 Wiki 文件，将新情报智能地“追加”并融合进原文件，保留人类原本的个性化修改，并更新 `last_compiled` 时间戳。
- 强制编织双向链接：每一个新编译的 Wiki 页面，内部必须包含至少 3 个双向链接（格式如 `[[目标Wiki文件名]]`）。如果提到了某个重要概念但该文件尚未创建，必须先写出占位符双链（如 `[[待创建的业务概念]]`）。
- 编译完成收尾动作：单次增量编译结束后，你必须自动对受影响的 `/raw/` 和 `/wiki/` 文件执行 `git add` 暂存动作。这能确保下一次编译时，这些文件不会再被误判为“新物料”，彻底节约大模型的 Token 损耗。

---

## 4. 知识检索与资料查阅规范 (Wiki-First Query Spec)

当人类向你查询资料、提问或者寻求业务解答时，你必须遵循以下 “Wiki 优先 + 穿透自愈” 的两阶段检索链路，绝对禁止盲目扫描全盘：

### 🔍 阶段一：高层地图检索 (Wiki-First Mode)
1. 优先检索：必须首先调用文件搜索工具，在 `/wiki/` 目录下进行关键词或语义检索。
2. 网状联想：利用 Obsidian 的双链依赖，顺着用户提问的节点，同时读取该节点关联的所有 `[[Concepts]]` 和 `[[Entities]]` 卡片。
3. 高密度输出：基于已经结构化、提炼好的 Wiki 内容，用最精炼、准确的语言在几秒钟内拼装出完整答案。

### 🚨 阶段二：深层穿透与知识自愈 (Breakthrough & Self-Heal)
1. 启动条件：当且仅当在 `/wiki/` 目录中找不到足够答案，或信息明显断层时触发。
2. 穿透查找：输入文本流检索工具，潜入底层的 `/raw/` 原始多媒体转录文本、PDF、文章仓库中进行全盘深度搜索。
3. 知识库自愈（最核心动作）：当你从 `/raw/` 的某篇旧材料中帮人类捞出答案并回答后，你必须立刻在后台启动一次增量编译——把这个之前漏掉的知识点重新提炼成 Markdown 写入 `/wiki/`，挂好对应的双向链接，并将其注册到 `index.md`。确保下次人类或你再次查询时，能直接在 `/wiki/` 中瞬间命中！

---

## 5. 知识库日常自愈维护 (Linting & Healing)

每次编译结束前，必须自动执行一次自愈检测：
1. 清理死链 (Dead Link Elimination): 如果发现某些 `[[双链]]` 指向的文件已经被改名或删除了，必须立刻修正该指针。
2. 拯救孤儿页面 (Orphan Page Recovery): 寻找全盘中入链数为 0 的页面。一旦发现，必须强制将其归类并挂载到 `/wiki/index.md` 主索引中。
3. 知识冲突熔断 (Contradiction Management): 如果新投喂的资料与 `/wiki/` 里原本记录的结论发生严重事实冲突，严禁自行决定去覆盖谁。必须立刻暂停该节点的编译，在项目根目录下生成一个 `CONTESTED.md`（冲突审核报告）文件，将双方的观点和出处原文列出，等待人类肉眼裁判。

---

## 6. 最高安全红线 - 铁律 (Strict Safety Guardrails)

- 🚫 严禁毁灭性清理: 你绝对被禁止使用任何带有毁灭性的文件系统命令（如 `rm -rf`、`git clean -fdx`、或者向整个文件夹写入空数据的清空逻辑）。
- 🚫 严禁越权修改环境: 严禁擅自修改、删除或污染项目根目录下的 `.gitignore`、`.opencode/`、`.claude/` 缓存以及系统原有的构建脚本。
- 🚫 事务一致性保障: 如果单次批量编译在中间因报错崩溃，你必须立刻使用本地 Git 工具（如 `git checkout /wiki`）对工作区进行回滚，确保知识库的网状图谱绝不发生逻辑坏死。
